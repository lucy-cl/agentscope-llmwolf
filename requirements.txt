# 狼人杀游戏 Agent 依赖包
# Werewolf Game Agent Dependencies

# 核心框架
# Core Framework
agentscope>=0.1.0

# 数据验证和结构化输出
# Data validation and structured output
pydantic>=2.0.0

# 数值计算
# Numerical computing
numpy>=1.24.0

# YAML 配置文件解析（支持从 config/agent_config.yaml 加载配置）
# YAML configuration file parsing (supports loading from config/agent_config.yaml)
pyyaml>=6.0

# 注意：
# Notes:
# 1. agentscope 是核心框架，需要从项目根目录安装或通过 pip 安装
#    agentscope is the core framework, install from project root or via pip
# 2. 必须设置环境变量 DASHSCOPE_API_KEY 以使用 DashScope 云端 API
#    Must set environment variable DASHSCOPE_API_KEY to use DashScope cloud API
# 3. Python 版本要求：>= 3.10
#    Python version requirement: >= 3.10
# 4. 配置加载优先级：环境变量 AGENT_CONFIG > config/agent_config.yaml > 默认提示词
#    Config loading priority: AGENT_CONFIG env var > config/agent_config.yaml > default prompt

